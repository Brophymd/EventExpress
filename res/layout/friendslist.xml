<?xml version="1.0" encoding="utf-8"?>


<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent">
    <ListView xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent"
              android:orientation="vertical"
              android:id="@+id/mainList"
              android:clickable="true"
              android:longClickable="true" android:layout_below="@+id/button_add">
    </ListView>
    <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:text="@string/lbl_Friends_List"
            android:id="@+id/textView_friendsList" android:layout_above="@+id/mainList"
            android:layout_alignParentLeft="true"/>
    <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_add"
            android:id="@+id/button_add" android:layout_alignParentTop="true" android:layout_alignParentRight="true"/>
    <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_viewRequests"
            android:id="@+id/button_viewRequests" android:layout_above="@+id/mainList" android:layout_toLeftOf="@+id/button_add"
            android:layout_marginRight="30dp"/>

</RelativeLayout>